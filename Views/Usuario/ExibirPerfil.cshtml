@model PontoDigital_final.ViewModels.UsuarioViewModel
@{
    ViewData["Title"] = "Perfil | Ponto Digital";
    ViewData["css"] = "perfil-style.css";
}
<main class="container">
    <div class="content">

        <h2>@Model.Usuario.Nome</h2>
        <div id="dados-perfil">
            <div>
                <p><b>Endereço:</b> @Model.Usuario.Endereco</p>
                <p><b>Telefone:</b> @Model.Usuario.Telefone</p>
                <p><b>Data de nascimento:</b> @Model.Usuario.DataNascimento.ToShortDateString()</p>
                <p><b>Empresa:</b> @Model.Usuario.Empresa.Nome</p>
                <p><b>CNPJ da empresa:</b> @Model.Usuario.Empresa.Cnpj</p>
            </div>
            
            <div id="flex-coluna">
                <p>Foto de perfil:</p>
                <img src="@Model.Usuario.UrlFoto" alt="Foto de Perfil" id="foto_perfil">
                <a href='@Url.Action("EditarPerfil","Usuario")' class="link">Editar Perfil</a>
            </div>
        </div>
    </div>
</main>